# 🎨 Frontend Context

## Purpose
A lightweight Vue.js SPA focused exclusively on UX, visualizations, and data presentation.

## 🛠️ Stack Specifics
- **Framework:** Vue 3 (Composition API)
- **Tooling:** Vite, TypeScript
- **Styling:** Bootstrap CSS (following Material Design principles)

## 📜 Strict Rules
1. **Dumb Frontend:** No complex business logic or financial math. All calculations happen in the backend.
2. **Explicit Typing:** TypeScript interfaces/types are mandatory for all API responses and component props.
3. **BFF Integration:** Consume aggregate endpoints from the backend to keep the UI responsive and simple.
4. **Localization:** Default to Indian Numbering System (Lakhs/Crores) using i18n wrappers.
5. **Security:** Never store sensitive financial data in local storage or unencrypted state.

## 🏗️ Architecture
- **State Management:** Use Pinia for global UI state if necessary, but prefer local component state for data-fetching results.
- **Service Layer:** Wrap API calls in a dedicated service layer to decouple components from the network logic.
