# Database Configuration
POSTGRES_USER=vibeuser
POSTGRES_PASSWORD=vibepass
POSTGRES_DB=vibedb
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Backend Configuration
SECRET_KEY=development-secret-key-change-me-in-production
# algorithm used for jwt encoding
ALGORITHM=HS256
# access token expire minutes
ACCESS_TOKEN_EXPIRE_MINUTES=30
ENVIRONMENT=development
# Origins for CORS (comma separated)
BACKEND_CORS_ORIGINS="http://localhost,http://localhost:8000"

# Traefik / Domain Configuration
DOMAIN_NAME=localhost

# Storage Configuration (LUKS Mount Point)
DATA_PATH=/mnt/vibe_data
